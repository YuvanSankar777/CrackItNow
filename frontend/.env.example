# Backend API base URL (must end with /api).
# Local dev leaves this as-is; in production set it to your deployed backend,
# e.g. https://crackitnow-backend.onrender.com/api
VITE_API_URL=http://localhost:8000/api
